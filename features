1. There is currently no way to remove time sieres once uploaded.
what if we just refresh the screen?
what if we just click and x and delete
2. labeler
some sort of box, highlight region, 
label boxes type of label notes field
save ->save to data frame ->upload to drive
box select tool
start date stop date and label
then start training models

what is coinbase and trading view using?